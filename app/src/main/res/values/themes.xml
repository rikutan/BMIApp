<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BMIApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BMIApp" parent="Base.Theme.BMIApp" />

    <!-- スプラッシュ画面用のテーマ -->
    <style name="AppTheme.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_bmi_calc</item>
        <!-- スプラッシュ画面後のテーマ -->
        <item name="postSplashScreenTheme">@style/Theme.BMIApp</item>
    </style>
</resources>
